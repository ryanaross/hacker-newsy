# Server / process
DEBUG=false
TZ=America/Los_Angeles

# SQLite database file (default: <cwd>/data/hn.db). Use :memory: for tests only.
# DB_PATH=./data/hn.db

# Docker only: map container user (handled by entrypoint.sh + su-exec)
# PUID=1001
# PGID=1001
